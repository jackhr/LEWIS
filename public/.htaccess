RewriteEngine On
RewriteBase /

# Keep real files and directories as-is.
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Send app routes to the SPA entry point.
RewriteRule ^ index.html [L]
